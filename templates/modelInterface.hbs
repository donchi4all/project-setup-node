/**
 * Interface representing a {{singular service}}
 */
export default interface I{{capitalizeSingular service}}Interface {
    id?: string;
    name: string;
    someOtherString: string;
    createdBy?: string;
    deletedAt?: string;
    createdAt?: string;
    updatedAt?: string;
}

/**
 * Type for creating a new {{singular service}}
 */
export type {{capitalizeSingular service}}CreationType = Pick<
    I{{capitalizeSingular service}}Interface,
    "name" | "someOtherString" | "createdBy"
>;

/**
 * Interface for bulk updating multiple {{plural service}}
 */
export interface I{{capitalizeSingular service}}BulkUpdateInterface {
    ids: string[];
    name: string;
    someOtherString: string;
}