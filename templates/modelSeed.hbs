'use strict';

/** @type {import('sequelize-cli').Migration} */
module.exports = {
  /**
   * Seed the {{plural service}} table
   */
  async up(queryInterface, Sequelize) {
    await queryInterface.bulkInsert(
      '{{plural service}}',
      [
        {
          id: '{{uuid}}',
          email: '{{email}}',
          password: '{{hashedPassword}}', // '{{plainPassword}}' (for reference)
          status: '{{status}}',
          createdAt: new Date(),
          updatedAt: new Date(),
        },
      ],
      {
        // Handle duplicates by updating these fields
        updateOnDuplicate: ['status', 'updatedAt'],
      }
    );
  },

  /**
   * Remove seeded {{plural service}} entries
   */
  async down(queryInterface, Sequelize) {
    await queryInterface.bulkDelete('{{plural service}}', null, {});
  },
};